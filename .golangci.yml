# golangci-lint configuration for rediver-sdk
# https://golangci-lint.run/

run:
  timeout: 5m
  modules-download-mode: readonly
  skip-dirs:
    - vendor
    - tmp
    - examples

output:
  formats:
    - format: colored-line-number
  print-issued-lines: true
  print-linter-name: true

linters:
  enable:
    # Security
    - gosec
    # Bugs
    - staticcheck
    - govet
    - errcheck
    - ineffassign
    - typecheck
    # Style
    - gofmt
    - goimports
    - misspell
    # Performance
    - prealloc
    # Complexity
    - gocyclo
    - funlen

  disable:
    - depguard

linters-settings:
  gosec:
    excludes:
      - G104  # Audit errors not checked
      - G304  # File path provided as taint input
    config:
      G301: "0750"
      G302: "0640"
      G306: "0640"

  funlen:
    lines: 150
    statements: 80

  gocyclo:
    min-complexity: 20

  misspell:
    locale: US

issues:
  exclude-rules:
    # Exclude test files from some linters
    - path: _test\.go
      linters:
        - gosec
        - errcheck
        - funlen

    # Exclude examples from most linters
    - path: examples/
      linters:
        - gosec
        - errcheck
        - funlen

  max-issues-per-linter: 50
  max-same-issues: 10
